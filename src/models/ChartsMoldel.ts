export interface ChartsMoldel{
    average_rating: number;
    counts: number[];
    negative_count: number;
    positive_count: number;
    timestamps: Date[];
    total_reviews: number;
    report: string;
}
